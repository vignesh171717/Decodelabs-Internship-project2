Developed a Machine Learning classification model using the K-Nearest Neighbors (KNN) algorithm and the Iris dataset. The project includes data preprocessing, feature scaling, train-test splitting, model training, prediction, and performance evaluation using confusion matrix, accuracy score, precision, recall, and F1-score. This project demonstrates the fundamentals of supervised learning and classification in Artificial Intelligence.
